password = input('Enter password: ')
password_ok = len(password) >= 8
print(f'Password length is valid: {password_ok}')


#czytanie hasła
#prawdzenie długości >= 8
#wynik True/False
#wypisanie rezultatu